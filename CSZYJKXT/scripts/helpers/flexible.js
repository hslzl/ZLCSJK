/**
 **日期：2014-08-07
 **作者：
 **功能：左侧信息显示框与地图容器框伸缩控制，左侧隐藏时，地图容器宽度为100%
 **      左侧显示时，地图容器margin左侧宽度；
 */
window.onload = function() {
	var mapinfo = document.getElementById("headerContainer");
	var ContentContainer = document.getElementById("ContentContainer");
	var h = window.innerHeight;
	//angular.element("#ContentContainer").height(h - 80);
}